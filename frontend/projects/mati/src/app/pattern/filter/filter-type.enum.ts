export enum FilterType {
  TEXT = 'text',
  SELECT = 'select',
  MULTI_SELECT = 'multi-select',
  RANGE = 'range',
  DATE = 'date',
  DATE_RANGE = 'date-range',
  BOOLEAN = 'boolean',
  NUMBER = 'number',
  SLIDER = 'slider',
}
